# Collect Pages (app/lobby.tsx, app/game.tsx, etc. Can be a folder)
# Then open component directory; for each file, find what page uses them
# if only 1 page uses it, move the file to the page's directory if the directory exists 
# otherwise create it and move the page file to index.tsx in the folder.

